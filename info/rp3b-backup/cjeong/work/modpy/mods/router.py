""" Router and worker nodes. """

import asyncio
import modpy


class RemoteResource(modpy.Resource):
    def __init__(self, name, handler):

def remote(fn):


@modpy.func
def add_worker(router, worker, resource):
    # add the resource to the router
    
